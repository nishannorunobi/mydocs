FROM python:3.12-slim

WORKDIR /doc-agent

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
RUN mkdir -p memory

EXPOSE 8893

CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8893"]
